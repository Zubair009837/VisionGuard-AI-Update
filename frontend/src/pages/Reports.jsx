import { useEffect, useState } from "react";
import api from "../services/api";
export default function Reports(){const [alerts,setAlerts]=useState([]);const [movements,setMovements]=useState([]);useEffect(()=>{Promise.all([api.get('/alerts'),api.get('/camera-movements')]).then(([a,m])=>{setAlerts(a.data||[]);setMovements(m.data?.history||[])}).catch(console.error)},[]);const download=()=>{const rows=[['Time','Type','Severity','Title','Description'],...alerts.map(a=>[a.time,a.type,a.severity,a.title,a.description])];rows.push(...movements.map(m=>[m.time,m.type,m.severity,m.camera,m.details]));const csv=rows.map(r=>r.map(v=>`"${String(v??'').replaceAll('"','""')}"`).join(',')).join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download='visionguard-report.csv';a.click();};return <div className="container-fluid px-4 py-4"><h2>Reports</h2><p className="text-muted">Export alert and camera-movement audit data.</p><button className="btn btn-primary mb-4" onClick={download}>Download CSV Report</button><div className="row g-3"><div className="col-md-6"><div className="card p-4 shadow-sm"><h4>{alerts.length}</h4><span>Alert events available</span></div></div><div className="col-md-6"><div className="card p-4 shadow-sm"><h4>{movements.length}</h4><span>Camera movement events</span></div></div></div></div>}
